extends ../layout 

block content 
  .container.my-4
    h1.text-center #{title}
    .container 
      .row 
        .col-4 
          img.img-fluid.mx-auto.d-block(src=service.imgUrl alt=service.name)
        .col-8 
          .container !{service.detail}
      
   
